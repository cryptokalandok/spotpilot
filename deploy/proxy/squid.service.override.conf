[Service]
# Force Squid itself to use IPv4, so the exchange sees the allowlisted IPv4.
# AF_UNIX is retained for local helpers and AF_INET for IPv4 network sockets.
RestrictAddressFamilies=AF_UNIX AF_INET
